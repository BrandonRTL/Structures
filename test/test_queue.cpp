#include "Uqueue.h"
#include <gtest.h>
/*
TEST(TQueue, can_create_queue)
{
	EXPECT_NO_THROW(Queue<int>(3));
}
TEST(TQueue, cant_create_queue_with_negative_size)
{
	EXPECT_ANY_THROW(Queue<int>(-3));
}
TEST(TQueue, can_push_element)
{
	Queue<int> A(10);
	A.push(3);

	EXPECT_EQ(3, A.top());
}
TEST(TQueue, can_pop_element)
{
	Queue<int> A(10);
	A.push(3);
	A.push(2);

	A.pop();
	EXPECT_EQ(2, A.top());
}
TEST(TQueue, returns_Is_Empty_1)
{
	Queue<int> A;

	EXPECT_EQ(1, A.IsEmpty());
}
TEST(TQueue, returns_Is_Empty_2)
{
	Queue<int> A;
	A.push(13);
	A.pop();

	EXPECT_EQ(1, A.IsEmpty());
}
TEST(TQueue, cant_pop_from_empty_queue)
{
	Queue<int> A;

	EXPECT_ANY_THROW(A.pop());
}
TEST(TQueue, cant_top_from_empty_stack)
{
	Queue<int> A;

	EXPECT_ANY_THROW(A.top());
}
*/